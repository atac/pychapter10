
[tox]
envlist = {py36,py37,py38}
skipsdist = true
skip_missing_interpreters = true

[testenv]
deps = -r requirements.txt
commands = pytest --junitxml=xunit-{envname}.xml --cov chapter10 --cov-report html

[pytest]
junit_family=xunit2
